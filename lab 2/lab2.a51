;Amanda Pan
;ID: 014551171
;Activity 2

mov a, #0 ; a <-# 0
add a, #1 ; a + #1
add a, #4 ; a + #4
add a, #5 ; a + #5
add a, #5 ; a + #5
add a, #1 ; a + #1
add a, #1 ; a + #1
add a, #7 ; a + #7
add a, #1 ; a + #1
mov r2, a ;r2 <- a
end


;Activity 1
;mov a, #0x99 ;a <- 99H
;mov r0, a  ;r0 <- a
;mov r1, a  ;r1 <- a
;mov r2, a  ;r2 <- a
;mov r3, a  ;r3 <- a
;mov r4, a  ;r4<- a
;mov r5, a  ;r5 <- a
;mov r6, a  ;r6 <- a
;mov r7, a  ;r7 <- a
;end
